version: '3.8'

services:
  erp-lumark:
    build: .
    container_name: erp-lumark-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
